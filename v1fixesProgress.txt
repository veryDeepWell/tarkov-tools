# v1 migration progress

Updated: 2026-09-22

## Rule

Always update this file after each migration batch. `v1fixes.txt` remains the architecture plan.

## Completed this batch (storage tails)

- `desk`: notify text no longer claims direct localStorage; already on TarkovState/TarkovStorage.
- `food`, `nvg`, `cultist`, `random-loadout`, `drip-loadout`: `TarkovStorage` for settings + preferred game mode.
- `lang-search` / `tarkov-localizer.js`: languages via `TarkovStorage.getJson/setJson`; HTML loads `tarkov-storage.js`.
- `drip-builder`, `loadout-budget` stubs: include `tarkov-storage.js` (no tool JS — intentional stubs).
- `tarkov-price-track-boot.js`: RUN/META keys via TarkovStorage.

## Current count

- Catalog tools: 46.
- Strict migration (TarkovStorage, no direct localStorage in tool JS): 44.
- Stub-only HTML (no JS to migrate): 2 — `drip-builder`, `loadout-budget`.
- Direct `localStorage` in tools/: 0 after boot fix.

## Remaining (not storage tails)

- Live platform hardening (poll/notify uniformity).
- i18n coverage on tool screens.
- Catalog single-source / generated artifact.
- Implementing stub tools when product-ready.
