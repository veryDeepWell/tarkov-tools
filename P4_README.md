# P4 — folder structure

Replace **entire** repo content with this tree (or merge carefully).

**Breaking:** catalog `file` paths are now `tools/tarkovtool-….html`.
Old bookmarks to root tool HTML will 404 — use hub.

Hub URL unchanged: `/tarkovtool-hub.html`
